/**
 * Enum for Minecraft version types.
 * - RELEASE: Official release versions.
 * - SNAPSHOT: Development snapshot versions.
 */
export enum VersionType {
  RELEASE = "release",
  SNAPSHOT = "snapshot",
}
